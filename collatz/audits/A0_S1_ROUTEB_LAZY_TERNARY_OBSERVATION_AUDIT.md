# DSD audit — lazy ternary observation and Bellman composition

Date: 2026-09-01

## Audited interface

The newly closed interface is

`final defect/correction residue mod 3^L`

→ `minimal current observation resolution`

→ `lazy projective residue R_q`

→ `augmented exact Bellman key`

while preserving the already-certified scalar physical danger label `P_min`.

## Layer separation

### Transition layer

Exact source future control and interval payload determine the future parity response to each exposed source-parameter bit.

This layer answers **what transition occurs**.

### Observation layer

For a final residue predicate `N_J mod 3^L`, the current prefix needs only

\[
R_q=N_q\bmod3^{m(q)},
\qquad
m(q)=\max(0,L-(J-q)).
\]

This layer answers **what ternary information is still observable at the terminal resolution**.

### Decision layer

The physical gate uses the scalar score

\[
P=m_{lo}N+\delta_{lo}3^qX_{lo}
\]

and only the minimum score per exact active key is needed.

This layer answers **whether the complete family can be rejected by the directed physical envelope**.

These three roles are not interchangeable.

## Exact consequences

1. If at least `L` future one-events remain, present prefix defect residue is invisible modulo `3^L`.
2. During the last `L` one-events, required precision grows exactly one trit per one-event.
3. For a fixed emitted one-event, backward residue propagation is empty or singleton.
4. Adding the exact lazy residue to the key does not require restoring a source/defect Pareto frontier; one `P_min` label remains sufficient for the combined directed physical + final-residue predicate.

## Current-scale consequence

For

\[
j_0=65,868,186,701
\]

and `L=28`, forward ternary observation is dormant through

\[
q\le65,868,186,673.
\]

Therefore a 28-trit terminal congruence cannot provide early forward pruning through the overwhelming majority of the pre bridge.

The same observation also means that carrying a 28-trit residue state from the 14 roots would be unnecessary state inflation.

## Rejected inference

The following inference is rejected:

`terminal 28-trit exposure exists`

→ `carry 28 trits from every 14-root state`

→ `expect early root pruning`.

The observation theorem proves the opposite information-flow fact: those trits do not see the early prefix until the last 28 one-events.

## Preferred next architecture

Use a two-front exact join.

### Forward front

Carry only

`source/control × interval payload × P_min`

while terminal ternary information is dormant.

### Backward right front

Start from the actual required terminal residue and propagate through the compressed right-side H/projective grammar.

For each fixed one-event/cylinder transition, the predecessor residue is unique or absent.

The backward object is therefore a residue-cylinder filter, not a cost-minimization process.

### Join

Join the backward admissible residue/control cylinders to the forward source/physical families at an exact block boundary.

No multiplication of marginal survival ratios and no independence assumption is permitted.

## Relation to the local-carry greedy no-go

The existing counterexample remains binding.

Backward singleton propagation is deterministic **after a branch/cylinder transition is specified**.

It does not justify choosing the branch itself greedily by maximizing the local exponent or minimizing the local defect.

Thus:

- branch/cylinder choice remains a grammar/control problem;
- residue predecessor within a chosen branch is deterministic;
- physical cost is still compared through `P_min`.

## DSD classification

### EXACT / CLOSED

- minimal lazy ternary resolution theorem;
- forward residue recurrence;
- backward empty-or-singleton predecessor;
- Bellman `P_min` preservation under augmented residue key.

### REGRESSION ONLY

Small first-defect/source-interval comparisons in the new certificate.

### SAFE STRATEGY CONSEQUENCE

Do not inflate the full forward 14-root state with terminal ternary residues before they become observable.

### OPEN

- identify the actual downstream residue(s) implied by each checkpoint/debit/tail predicate;
- build the backward compressed H/projective suffix filter for those residues;
- prove an exact forward/backward join state at the chosen block boundary;
- execute the join on the 14-root forest.

### NOT CLAIMED

- closure of any root;
- full Route-B closure;
- C4F preservation;
- Collatz.
