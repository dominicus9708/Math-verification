# S10 audit — five-displacement horizon pruning

Audit tuple: `(D,R,S,E,T,C,N,O)`.

## Result

\[
\boxed{H_4=50},
\qquad
\boxed{D_{51}\ge5},
\qquad
\boxed{\eta_{future}>5/12}.
\]

Safe endpoint weakening:

\[
\eta_{future}\ge5/12.
\]

This replaces, rather than adds to, the previous `>=1/3` future-defect floor.

The exact endpoint update is

\[
26{,}859{,}837{,}368{,}506{,}133{,}665
\longrightarrow
26{,}859{,}837{,}368{,}480{,}843{,}030,
\]

so the incremental rejection is

\[
\boxed{25{,}290{,}635}.
\]

No whole source interval closes; `14,224` intervals remain.

## D — domain

**CLOSED.**

The computation is restricted to the current canonical `A0, s=1, Route-B` jump-8 source frontier after the certified `>=1/3` cut.  It is not promoted to other sectors.

## R — resolution

**CLOSED.**

Every search state preserves an exact nonempty affine source interval.  The search resolution is the exact source-child refinement, not a marginal density or sampled orbit.

## S — state sufficiency

**CLOSED for this predicate.**

The reachability recursion uses the exact affine current channel `(y,lo,hi,h,q)` obtained from source-preserving refinement.  No coordinates required by the bounded-displacement/pure-ballot predicate are discarded.

This sufficiency claim is local to this predicate; it does not authorize source merging for later checkpoint predicates.

## E — equivalence / merge legality

**CLOSED / no merge used.**

No two source payloads are identified merely because their control state agrees.  `source_payload_merging_used=False` in the execution certificates.

## T — transition exactness

**CLOSED.**

Every displacement branch is generated by the exact source-child congruence.  No horizon interpolation or linear extrapolation is used.

At horizon 49 the only live shards are exactly 3 and 24.  Their direct continuation gives maximum horizons 49 and 50 respectively; all other shards are already empty at 49.  Hence the global maximum is exactly 50.

## C — closure logic

**CLOSED for the finite implication.**

`H_4=50` means no path with at most four displaced ranks reaches horizon 51.  Therefore every horizon-51 survivor has at least five displaced ranks.

The established strict per-displacement lower bound `>1/12` then yields `eta_future>5/12`.  Weakening to `>=5/12` for a monotone endpoint cut is safe.

This is not a whole Route-B closure theorem.

## N — non-independence / double counting

**CLOSED.**

The `1/4`, `1/3`, and `5/12` floors describe successive lower bounds on the same future defect quantity.  They are not independent.  The newest `5/12` cut supersedes the older floors.

The reported `25,290,635` is the exact set difference between the `>=1/3` and `>=5/12` endpoint cuts, not a product or sum of survival fractions.

## O — outstanding obligations

**OPEN.**

1. Compute `H_5` or a stronger scalable bounded-displacement theorem without extrapolation.
2. Determine whether cumulative displacement pruning remains worthwhile relative to its execution cost.
3. Continue the independent source-sensitive long-membership/checkpoint obstruction, especially in the permanently `P_min`-unreachable sector.
4. No statement here closes `A0,s=1,Route-B`, Route-A, `s>=2`, or Collatz globally.

## DSD verdict

- finite `H_4=50`: **EXACT / CLOSED**;
- `D_51>=5`: **EXACT / CLOSED**;
- `eta_future>5/12`: **SAFE consequence of closed finite result**;
- endpoint population update: **EXACT finite arithmetic**;
- whole-family closure: **0 / OPEN**;
- global Collatz implication: **REJECTED**.
