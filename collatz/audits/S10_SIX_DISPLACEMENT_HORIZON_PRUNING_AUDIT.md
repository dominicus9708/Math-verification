# S10 audit — six-displacement horizon pruning

Audit tuple: `(D,R,S,E,T,C,N,O)`.

## Result

Exact finite reachability result on the current `>=5/12` frontier:

\[
\boxed{\#\{D_{51}\le5\text{ paths}\}=0}.
\]

Therefore

\[
\boxed{H_5\le50},
\qquad
\boxed{D_{51}\ge6},
\qquad
\boxed{\eta_{future}>1/2}.
\]

Safe endpoint weakening:

\[
\eta_{future}\ge1/2.
\]

The exact endpoint update is

\[
26{,}859{,}837{,}368{,}480{,}843{,}030
\longrightarrow
26{,}859{,}837{,}368{,}455{,}538{,}464,
\]

so the incremental rejection is

\[
\boxed{25{,}304{,}566}.
\]

No whole source interval closes; `14,224` intervals remain.

## D — domain

**CLOSED.**

The reachability computation is restricted to the canonical `A0, s=1, Route-B` jump-8 source frontier after the certified `>=5/12` endpoint cut. No result is promoted to Route-A, `s>=2`, or a global Collatz domain.

## R — resolution

**CLOSED.**

The 64-shard decision covers all `14,224` exact source parents. Each recursive state is obtained from source-preserving affine refinement; no orbit sampling or density approximation is substituted for family reachability.

GitHub Actions run `33756884264` reports `parent_count=14224` and `parents_with_c5_path_h51=0`.

## S — state sufficiency

**CLOSED for the bounded-displacement predicate.**

The decision recursion retains the exact affine current channel required to determine legal source children, pure-ballot admissibility, and displacement count. This local sufficiency does not authorize dropping source residue `r` for later checkpoint membership predicates.

## E — equivalence / merge legality

**CLOSED / no source merge used.**

The result does not identify two source families because of equal control states, signatures, corrections, or residues. The aggregate counts decisions over exact source parents.

## T — transition exactness

**CLOSED.**

Every branch is generated by the exact source-child congruence. The horizon is fixed at 51; there is no interpolation from the known `H_0..H_4` values and no extrapolation to higher budgets.

## C — closure logic

**CLOSED for the finite implication.**

No path with at most five displaced ranks reaches horizon 51. Hence any horizon-51 survivor must contain at least six displaced ranks:

\[
D_{51}\ge6.
\]

With the independently certified strict contribution `epsilon_r>1/12` per displaced rank,

\[
\eta_{future}>6/12=1/2.
\]

Weakening `>1/2` to `>=1/2` for the monotone endpoint cut is safe.

The same data prove only `H_5<=50`, not `H_5=50`. An equality claim without a horizon-50 witness is **REJECTED**.

## N — non-independence / double counting

**CLOSED.**

The `1/4`, `1/3`, `5/12`, and `1/2` floors are successive lower bounds on the same future-defect quantity. They are nested and are not summed or multiplied as independent filters.

The reported `25,304,566` is the exact set difference between the `>=5/12` and `>=1/2` endpoint cuts recomputed from the same first-75 source intervals.

## O — outstanding obligations

**OPEN.**

1. Determine whether `H_5=50` is actually attained if that equality is useful; it is not needed for the current `D_51>=6` consequence.
2. Do not assume continued bounded-displacement expansion will become a whole-family closure mechanism; all `14,224` intervals remain live.
3. Prioritize the independent exact source/checkpoint join:
   \[
   \text{source family}\leftrightarrow(z_2,z_H)\leftrightarrow\text{right-H/checkpoint family}.
   \]
4. Require source/debit compatibility and same-orbit provenance after CRT exposure.
5. `A0,s=1,Route-B`, all other sectors, and Collatz remain OPEN.

## DSD verdict

- horizon-51 `c=5` non-reachability: **EXACT / CLOSED finite frontier**;
- `H_5<=50`: **EXACT**;
- `H_5=50`: **OPEN / not claimed**;
- `D_51>=6`: **EXACT / CLOSED**;
- `eta_future>1/2`: **SAFE consequence**;
- `>=1/2` endpoint arithmetic: **EXACT**;
- whole-family closure: **0 / OPEN**;
- global Collatz implication: **REJECTED**.
