# Dated mainline proof tree

This file records the current proof route from older structural work to the present compact-carry frontier. Dates are ordered from older to newer. Exact barrier results that changed the route are retained as pivot nodes.

## 2026-08-09 — structural precursor layer

The first organized branch studied survivor structure, resonance, entropy, record growth, min-plus reductions, and endpoint merging. These files are now mainly historical/supporting rather than direct terminal dependencies, but they generated the later need for exact address-preserving state.

See `side-branches.md` for the preserved file inventory.

## 2026-08-10 — prefix / first-merge layer

The `deltaq1`, first-merge gap, and prefix-channel calculations refined how distinct prefixes can merge and how much endpoint information survives.

Current role: precursor to endpoint/Hensel/same-integer analysis.

## 2026-08-11 — Beatty / Christoffel / renewal / ceiling layer

The language was reorganized through boundary locking, ceiling gaps, Christoffel defects, renewal blocks, contracting macroblocks, and critical-strip calculations.

Current role: historical structural route. The lasting methodological consequence is that a coarse symbolic language is not sufficient without ordinary-integer compatibility.

## 2026-09-07 to 2026-09-08 — first universal cell and the 340-block frontier

### MATH-004 — endpoint q-lock — `MAINLINE`

Within the audited first-crossing candidate language, equal endpoint forces equal `q`.

### MATH-005 — 340-block endpoint locality — `MAINLINE`

Same-endpoint starts differ by less than `2^35`, so one endpoint fiber can couple at most two adjacent `2^61` address blocks.

### MATH-006 — exact 61+11 address transducer — `MAINLINE`

Write

```text
N = a*2^61 + x,  1024 <= a <= 1363.
```

The lower-61 state and the final 11 parity bits are connected by the exact lifted residue `(y + a*3^q) mod 2048`.

### MATH-007 — right-congruence barrier — `BARRIER`

The strong low-surplus range does not admit a useful exact quotient that forgets endpoint phase.

Pivot: retain phase information.

### MATH-008 — actual lower-61 phase reachability — `BARRIER`

All 2048 phases actually occur wherever MATH-006 can reject labels (`q61=39..45`).

Pivot: phase sparsity cannot be converted into fixed global block exclusions.

### MATH-009 — Hensel/endpoint ordering redundancy — `REDUNDANT PIVOT`

Inside a fixed `(k,q,E)` endpoint fiber, minimum-start ordering and correction-max ordering are the same affine order in opposite coordinates.

Pivot: do not count them as independent filters.

### MATH-010 — DSD-native representation gate — `MAINLINE SAFETY`

The transition `BASE_ENDPOINT -> ADDRESS_LIFTED` is allowed exactly once. This prevents double application of the top-address affine term.

### MATH-011 — complete descriptor and cyclic-window acceleration — `SUPPORT / ACTIVE ACCELERATION`

The 11-step tail is represented by an exact threshold descriptor, and the 340-address progression becomes a cyclic contiguous window. This reduces the expensive predicate-evaluation layer by an exact factor of 340 without strengthening the theorem.

### MATH-012 — Hensel even-budget descriptor — `MAINLINE GATE WITH LIMITED SCOPE`

The frozen-floor arithmetic-credit predicate is exactly

```text
k - q <= 71
```

and

```text
Q_*(k) = max(q_min(k), k-71).
```

Failure of this gate is not failure of a Collatz candidate.

## 2026-09-11 — transition from step accounting to paid macros

### MATH-058 — paid-macro transition bound — `HISTORICAL MAINLINE PIVOT`

Exact endpoint parity is added to the macro boundary state. A repeatable internal one-paid macro has zero-cost length at most 69, giving the conservative global accounting

```text
K <= 72 P + 89.
```

With the earlier paid-event cost this strengthens the cumulative penalty bound.

Pivot: scalar worst-case macro slope remains insufficient to close the first cell; the next route must use weighted/exact macro transitions and same-integer address information.

## 2026-09-12 — exact dyadic carry/address channel

### MATH-090 — dyadic carry-valuation compatibility — `MAINLINE`

Terminal same-integer compatibility is rewritten in finite dyadic carry/valuation form.

### MATH-091 — carry-transfer transducer — `MAINLINE`

For current family `(B,Q,M)`, exact compatibility residue and carry are transported into the child state:

```text
(B,Q,M) -> (r,d) -> (B',Q',M').
```

This makes same-integer address information iterative rather than terminal-only.

### MATH-092 — normalized 2-adic address transducer — `MAINLINE`

The large affine address is converted into a normalized 2-adic transition suitable for exact composition.

### MATH-093 — dyadic resolution envelope — `MAINLINE SUPPORT`

The source family is organized by `R = ceil(log2 M)` for proof-facing resolution control.

### MATH-094 — resolution-indexed danger kernel — `BARRIER`

The address-forgotten phase danger kernel saturates.

Pivot: phase plus resolution is insufficient.

### MATH-095 — phase-resolution Bellman negative result — `BARRIER / MAINLINE PIVOT`

Even after retaining exact phase and accumulated positive penalty, every one of the 857 initial states retains a fictitious negative path in the address-forgotten envelope.

Therefore

```text
(R, phase, penalty)
```

is not a sufficient proof-facing state. An exact carry/address channel is mandatory.

## 2026-09-13 — finite future precision and compact-carry closures

### MATH-096 — normalized Hensel quotient and finite future precision — `MAINLINE`

For an exact composed cylinder,

```text
X = 3^{-Q} B = (A+S)/2^H.
```

For source-family resolution `R = ceil(log2 M)`, all future one-paid address decisions need only

```text
P(R) = 73 + R
```

2-adic bits. The future-complete compact address state is

```text
X mod 2^(73+R),
G=3^{-Q} mod 2^(73+R),
```

plus exact multiplicity, phase, and source-modulus depth information.

### MATH-097 — t=16 — `CLOSED`

Danger threshold `z=h-R >= 48`; 45,094,414 danger-edge attempts; 0 address-compatible danger edges; minimum rejected residue gap 197,239,627.

### MATH-098 — t=15 — `CLOSED`

Exact compact-carry/address closure.

### MATH-099 — t=14 — `CLOSED`

Exact compact-carry/address closure.

### MATH-100 — t=13 — `CLOSED`

`z>=39`; 622,022,028 attempts; 0 compatible; minimum rejected gap 83,441.

### MATH-101 — t=12 — `CLOSED`

`z>=36`; 886,709,993 attempts; 0 compatible; minimum rejected gap 1,794.

### MATH-102 — t=11 — `CLOSED`

`z>=33`; 991,302,455 attempts; 0 compatible; minimum rejected gap 514.

### MATH-103 — t=10 — `CLOSED`

`z>=30`; 300 actual initial danger roots; 882,659,777 attempts; 0 compatible; minimum rejected gap 548.

## Current endpoint

```text
unresolved detailed one-paid band = 7 <= t <= 9
```

Natural next sequence:

1. build the exact phase-danger corridor for `t=9`;
2. replay it with the finite compact carry/address state;
3. test whether any Bellman-danger terminal edge is same-integer compatible;
4. if closed, repeat for `t=8` and `t=7`;
5. after the one-paid band is closed, audit the remaining paid-count layers and the complete first-cell implication chain separately.

Closing this finite band alone is not a Collatz proof.
