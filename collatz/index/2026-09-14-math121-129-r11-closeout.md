# 2026-09-14 — MATH-121 through MATH-129 r=11 closeout snapshot

Status: `MAINLINE STRUCTURAL AUDIT / r=11 OPEN / NO FIRST-CELL CLOSURE CLAIM`

## Certified frontier

```text
one-paid detailed band t=7..16   CLOSED on audited Bellman/address criterion
multi-paid r>=12                 CLOSED
multi-paid r=11                  OPEN — MATH-116 exact closure workflow still incomplete
multi-paid r=10                  OPEN — MATH-117 execution-ready, not launched
multi-paid 2<=r<=9               OPEN
first universal Farey cell       OPEN
Collatz conjecture                OPEN
```

MATH-111 has certified `r=12 CLOSED`. MATH-116 run `34768752143` has a successful preparation certificate and multiple successful exact AP-union shards, but later shards remain runner-queued. No observed failure is promoted to a mathematical conclusion; `r=11` remains OPEN until a complete exact certificate exists.

## MATH-121 — scalar rank barrier

The normalized MATH-120 floor state is

```text
z = 2^d/3^q,
u = c/3^q,
F = z*2^71 - N - u.
```

`F>=0` is exact frozen-floor closure for the branch, but `F` alone is not monotone: an even step raises it and an odd step lowers it. Moreover `-F/z` is just the current branch value excess above `2^71`, so a scalar function of `(z,F)` does not create an independent well-founded mechanism. Exact parity/address structure remains necessary.

Classification: `BARRIER / MAINLINE PIVOT`.

## MATH-122 / MATH-123 — exact non-singleton AP ranks

For an exact AP branch with current maximum value `N` and multiplicity `m>=2`, one parity refinement gives

```text
m' <= ceil(m/2) < m.
```

MATH-122 uses

```text
V=(N+1)m^2
```

as a strictly decreasing positive integer rank on every non-singleton branch.

MATH-123 strengthens this to the lexicographic rank

```text
W=(N+1)m,
rank=(W,m).
```

`W` never increases under one exact parity refinement, and every equality case has strictly smaller `m`; hence `(W,m)` is strictly decreasing lexicographically. This proves finite AP resolution without invoking average drift.

Important boundary: the rank controls resolution to singleton state. It does not by itself prove eventual descent below the frozen floor after singleton resolution.

Classification: `MAINLINE EXACT RESOLUTION LEMMA`.

## MATH-124 — direct parity-word AP address theorem

For a feasible parity word `w` of length `d`, odd count `q`, and affine correction `c_w`, its source residue is

```text
r_w = -c_w * (3^q)^(-1) mod 2^d.
```

For an odd-step AP

```text
n=a+bk,
```

with odd `b`, the exact source-parameter address is

```text
k = (r_w-a)b^(-1) mod 2^d.
```

Thus a parity word can be mapped directly to the exact parameter residue without recursively materializing every intermediate split.

Classification: `MAINLINE EXACT ADDRESS LEMMA`.

## MATH-125 — sharp correction envelope and q-gate

For all parity words of length `d` with exactly `q` odd steps,

```text
3^q-2^q <= c_w <= 2^(d-q)(3^q-2^q),
```

and the upper bound is attained by placing the odd steps in the last `q` positions.

Therefore every such word for source maximum `N` is frozen-floor safe when

```text
(3^q/2^d)N + (3/2)^q - 1 <= 2^71.
```

Equivalently define the exact source-maximum threshold

```text
L(d,q)=floor((2^d*2^71-c_max(d,q))/3^q).
```

The threshold decreases with `q`, yielding an exact address-forgotten safe odd-count prefix gate. High-`q` tails still require exact address/correction information.

Classification: `MAINLINE SUFFICIENT FLOOR GATE`.

## MATH-126 through MATH-128 — intermediate r=11 safe-mass audits

These calculations applied the MATH-125 gate to the exact MATH-116 prepared source representation.

They remain valid historical finite certificates but are superseded as the active pruning bound by MATH-129:

```text
MATH-126  common-depth / complete-block lower bound       ~82.2133%
MATH-127  d=22 exact remainder-aware bound               ~84.930482%
MATH-128  piecewise hybrid bound                         ~87.3529468%
```

No percentage is used as a density-to-universality argument. Each value counts an explicitly certified subset of source parameters.

## MATH-129 — current strongest exact r=11 q-gate union

For each source residue `r mod 2^22`, let `q_d(r)` be its exact odd-step count through depth `d`. Define

```text
N_*(r)=max_{1<=d<=22} L(d,q_d(r)).
```

A prepared AP piece with source maximum `N` is safe on that source residue iff

```text
N <= N_*(r)
```

for at least one `d<=22`. This forms the logical union first and counts every safe source parameter exactly once.

Exact result:

```text
prepared split pieces              605,977
total source occurrence mass       3,419,719,061,560
threshold levels                   275
exact multi-depth safe mass        3,209,065,424,947
exact uncertified tail mass        210,653,636,613
safe fraction                      93.840030925905822%
```

The earlier adaptive complete-block method through depth 34 beats MATH-129 on zero AP pieces; MATH-129 wins on 601,534 pieces and ties on 4,443.

Authoritative artifacts:

```text
collatz/src/2026_09_14_math129_r11_multidepth_qgate_audit.cpp
collatz/results/2026-09-14-math129-r11-multidepth-qgate.tsv
collatz/notes/2026-09-14-math129-r11-exact-multidepth-qgate.md
```

Classification: `MAINLINE FINITE AUDIT / CURRENT STRONGEST q-GATE SAFE-MASS BOUND / r=11 OPEN`.

## Claim boundary

The remaining `210,653,636,613` source occurrences are not counterexamples and are not claimed to survive indefinitely. They are only the part not discharged by the MATH-125 envelope at any prefix depth through 22.

Consequently:

```text
93.8400% exact safe subset  !=>  r=11 CLOSED
r=11 CLOSED                 !=>  first universal cell CLOSED
first universal cell CLOSED !=>  full Collatz proof without the separate universal reduction theorem
```

## Next exact continuation

The next mathematically clean target is only the MATH-129 tail. Acceptable routes are:

1. deeper exact multi-depth gates;
2. MATH-124 exact source-address/correction evaluation on the high-odd-count tail;
3. completion of the unchanged MATH-108 AP-union engine through MATH-116.

After all multi-paid layers are discharged, a separate paid-layer coverage audit and first-cell implication-chain audit remain mandatory.