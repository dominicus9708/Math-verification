# Canonical live proof stack

This is the shortest current live implication stack.  It deliberately omits historical exploratory routes and implementation regressions.

A line appearing here means only that the stated implication has an exact or explicitly SAFE certificate in its stated domain.  The final line is still OPEN.

## S0 — Physical A0 shell and fixed checkpoint data

Work in the certified A0 `s=1` formation with the fixed long pre-bridge parameters and physical X/Z corridors used by the current Stage-4 branch.

Status: **DOMAIN / imported**.

## S1 — Near-threshold finite closure

Independent finite certificates close Hamming radius `0..7` in the first-75 threshold window.

Therefore every remaining Route-B survivor satisfies

`d_75 >= 8`.

Status: **CERTIFIED ARITHMETIC → SAFE NECESSARY CONDITION**.

## S2 — Monotone defect and refined physical X bound

Pure-ballot/dominance order makes threshold correction maximal.  The first-75 defect is nonnegative and irreversible under continuation.

Together with the Christoffel real envelope this yields the refined necessary X bound.

Status: **EXACT monotonicity + CERTIFIED arithmetic → SAFE pruning**.

## S3 — Exact 14-root source forest

Late first-defect shells are closed and every remaining physical candidate lies in exactly one arithmetic source root with first threshold disagreement

`F14 = {2,5,8,10,13,16,18,21,24,27,29,32,35,37}`.

Each root carries an exact finite integer parameter interval and exact bit-refinement rule.

Status: **EXACT search-family representation after SAFE upstream pruning**.

## S4 — Family structural compression

The long word families admit exact compressed representations:

- source affine cylinders;
- exact ballot/dominance states;
- dual H/L canonical grammar;
- target-only Stern-Brocot alignment;
- dyadic left-front and ternary right-front localization;
- projective carry/displacement cylinders;
- finite interval payload quotient.

Status: **EXACT structural lemmas**, with adaptive/global membership composition still restricted by later gates.

## S5 — Structural restriction to actual defect

A projective successor state plus formation/order interval selects an exact candidate-position cylinder.  Its maximal legal position gives the exact minimum local normalized correction defect.

Defects compose positively through the normalized semiring.  Fixed projective-cylinder sequences admit exact ordering-aware minimum-defect evaluation.

Status: **EXACT / CLOSED locally**.

## S6 — Defect to physical family closure

The directed real envelope converts a certified normalized-defect floor into a physical X upper bound.  Equivalently one may compare the structural defect state against an exact closure budget determined by the source interval lower endpoint.

Recent min-plus reductions package the active physical risk into a monotone state so that dominated histories can be merged inside an exact future-control class.

Status: **EXACT closure criterion and state-dominance rule**.

## S7 — Current active computation

Run the exact source/refinement forest with the smallest sufficient active state:

`source control × interval payload × minimum physical-risk/defect label`

and introduce checkpoint / ternary projective / renewal predicates lazily when the unresolved membership gate actually queries them.

Goal: close whole descendant families before singleton expansion whenever the certified physical/membership criterion permits.

Status: **ACTIVE / not yet globally executed across all 14 roots**.

## S8 — A0 `s=1` Route-B closure

Required conclusion:

Every family generated from all 14 roots is closed or exactly discharged through the full pre/tail/checkpoint membership obligations.

Status: **OPEN**.

## S9 — Global completion

Route-A, `s>=2`, remaining formation branches, and the global completeness map must also be closed.

Status: **OPEN**.

---

# Forbidden shortcuts

The following do not belong in the canonical implication chain:

- `adic mismatch => membership rejection`;
- interval inclusion => correction-language membership;
- endpoint residue exposure => same orbit;
- local carry greedy => global minimum defect without a fixed cylinder sequence;
- multiplication of marginal survival ratios without independence;
- finite small-horizon agreement => universal theorem;
- `A0, s=1, Route-B` closure => Collatz without C0/C6.

Rejected routes remain valuable audit records but are kept outside this live stack.
