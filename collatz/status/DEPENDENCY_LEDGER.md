# Dependency ledger

This file records dependency order that must not be silently reversed.

## D1 — Radius-seven closure before refined X bound

Required order:

`old X bound` -> `finite radius 0..7 closure` -> `d75 >= 8` -> `first-75 defect floor` -> `Christoffel real-envelope conversion` -> `refined X upper bound`.

Rule: the refined X bound is not used retroactively to justify radius-seven closure unless separately rerun.

## D2 — Refined X bound before shell-conditioned pruning

Shell-specific physical restrictions derived from the refined interval remain downstream of that interval.

## D3 — 14-root forest after late-shell elimination

The 14-root forest is retained only after certified late-shell elimination.

## D4 — H/L grammar before target specialization

Universal dual H/L grammar precedes target Christoffel/Stern-Brocot specialization.

## D5 — Projective observation before membership use

Required interface:

`projective observation` -> `formation/order restriction` -> `ordinary displacement/defect or exact language equality` -> `legal pruning/membership conclusion`.

Rejected shortcut: `adic mismatch -> membership rejection` outside an explicit active equality predicate.

## D6 — Local defect floors before global accumulation

Local floors are not automatically additive. Use ranked atoms, fixed-cylinder ordering minimum, or a certified semiring/block composition.

## D7 — Fixed-cylinder greedy only after cylinder sequence is fixed

Right-to-left maximal-position greedy is exact inside an already fixed cylinder sequence, not for selecting the carry/cylinder path.

## D8 — Bellman merge only inside exact future-control classes

One `P_min` label is legal only when histories share the exact future control/payload state required by the active physical theorem.

At ordinary-X exposure depth `h=72`, do not assume that a physical-predicate merge remains sufficient for arbitrary later membership predicates.

## D9 — Synchronized checkpoint reconstruction

The checkpoint dyadic and ternary observations are residues of the same ordinary checkpoint `Z`.

Closed order:

`independent pre-defect X/L_- corridor` -> `SAFE Z corridor` -> `full right-H affine z_H <-> Z mod3^28` -> `Z mod2^27` -> `coprime CRT` -> `corridor span < CRT modulus` -> `at most one ordinary Z`.

No marginal-density multiplication or independence assumption is permitted.

## D10 — Full CRT singleton requires a full ternary observation

The synchronized singleton theorem is conditional on a **full** supplied value of `z_H mod3^28`.

The terminal target-dominance existence theorem supplies only

\[
z_H\bmod3\in\{0,1\}
\iff
Z\bmod3\in\{1,2\}.
\]

Rule: do not promote that automatic first-trit condition to a full `3^28` observation and do not invoke checkpoint singleton exposure from it.

The weak channel `Z mod2^27 × Z mod3 in {1,2}` has modulus `3*2^27` and is non-isolating on the debit interval.

## D11 — No retroactive defect bound in checkpoint CRT seam

The checkpoint singleton theorem uses only the independent pre-defect `X` and debit corridors, not the later defect-derived refined X upper bound.

## D12 — Checkpoint exposure before checkpoint-conditioned source fiber

Only after one ordinary `Z` is independently exposed may the root cylinder `X=r+2^h m` be intersected with

\[
75\,2^{33}<3X-Z<112\,2^{33}.
\]

The exact root-depth cap is

\[
K_h=\left\lceil\frac{37\,2^{33}}{3\,2^h}\right\rceil.
\]

Small fiber cardinality is not membership.

## D13 — Debit reconstruction without circularity

`L_-=3X-Z` is used only after `Z` is independently exposed and `X` remains in its independently defined source cylinder.

## D14 — Terminal precision and ranked-one locality before right-H state design

For a terminal `3^L` predicate:

1. only the final `L` ranked one-events affect the correction difference modulo `3^L`;
2. a processed right block with `q_B` one-events exports only `max(0,L-q_B)` ternary digits.

At the current right-H block, `q_B=397,573,380`, so `L=24,28,47` export zero ternary digits at the cut.

Rule: do not carry a discharged checkpoint ternary state across the cut and do not rebuild the unobservable 397M-one prefix merely for the terminal residue predicate.

## D15 — Terminal target-dominance saturation before pruning claims

For the current terminal 28-rank target-dominance problem, the exact lifting threshold is

\[
T_0=82
\]

while all actual capacities exceed `232,565,502`.

Thus target-dominance suffix existence is equivalent to

\[
z_H\bmod3\in\{0,1\}
\iff
3\nmid Z.
\]

Every genuine positive-one-count checkpoint already satisfies `3∤Z` from the correction identity modulo 3.

Rule: terminal target-dominance existence is a consistency theorem, not an independent pruning dependency. Do not reactivate it as a carry-enumeration search engine.

Canonical theorem/audit:

- `../theorems/TERMINAL_28GATE_DOMINANCE_SATURATION.md`;
- `../audits/TERMINAL_DOMINANCE_GATE_REDUNDANCY.md`.

## D16 — Source-controlled correction target before full membership

For a proposed `(X,Z)`, define

\[
C_{req}=2^{t_0}Z-3^{j_0}X.
\]

For every `K<=t_0`,

\[
C_{req}\equiv-3^{j_0}X\pmod{2^K}.
\]

This source-controlled dyadic observation may be used to constrain correction-language inversion.

Required order:

`source prefix/control`
-> `required correction observation`
-> `exact block/H-L correction-language realization`
-> `existence or rejection`
-> `fixed-(t0,j0) injectivity for uniqueness after existence`.

Rule: correction injectivity does not imply existence; a matching residue does not imply full membership.

## D17 — Physical execution evidence does not change theorem scope

Finite exact `P_min` scans for `f=29,32,35,37` produced zero whole-family physical closures through `h=72`.

Rule: these are search-engine/execution observations only. They neither prove universal non-closure nor authorize discarding the physical predicate when combined with a stronger source-controlled state.

## D18 — Local Route-B closure before global claims

Even complete closure of all 14 `A0, s=1, Route-B` roots would close only that module.

Required later dependencies:

`Route-B local closure` -> `Route-A + s>=2 + remaining branches` -> `global branch completeness` -> `Collatz conclusion`.

## Change-control rule

Whenever a new theorem shortens the chain:

1. state exactly which prior obligation is discharged;
2. preserve old chronological/execution evidence;
3. update `CANONICAL_PROOF_STACK.md`, `OPEN_GATES.md`, and the active frontier in the same maintenance cycle;
4. do not silently reinterpret old finite results under new assumptions.