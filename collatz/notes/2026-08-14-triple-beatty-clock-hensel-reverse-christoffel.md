# Triple Beatty-clock unification: Christoffel gaps, reverse contraction, and Hensel integerization

Date: 2026-08-14

Status: **exact algebraic identification of three previously separate admissibility clocks**.  This is a structural theorem inside the proof program, not a Collatz proof.

Put

\[
\gamma:=\log_2(3/2).
\]

## 1. Odd-only mechanical clock

For the odd-only mechanical / Christoffel reference, the accumulated extra powers of two above the mandatory one-per-odd baseline are governed by

\[
\boxed{
\lfloor d\gamma\rfloor
}
\]

and the local valuation word is generated by the rises of this Beatty staircase.

This is the established odd-gap / skew clock.

## 2. Common-OO reverse contraction clock

In the aligned common-`OO` alternate-preimage sieve, after `d` effective reverse odd steps and `E` extra inverse-even doublings, multiplicative contraction relative to the original start is

\[
2^{d+E}<3^d.
\]

Equivalently,

\[
E<d\gamma.
\]

Since `d gamma` is irrational, the largest allowed integer budget is

\[
\boxed{
E_{\max}(d)=\lfloor d\gamma\rfloor.
}
\]

This is why new prefix-minimal reverse-forbidden ternary cylinders can be born only on rises of the same Beatty staircase.

## 3. Binary Hensel integerization clock

Now consider a length-`L`, odd-count-`q` alternate parity word.  Let

\[
E:=L-q
\]

be its total number of even symbols.

Suppose a correction difference has

\[
s=v_3(C)=q-d.
\]

The reduced rational alternate start has denominator `3^d`; it becomes ordinary after the `d`-th alternate odd symbol.

For fixed total `L,q,d`, the latest possible time of that `d`-th odd symbol is obtained by placing the remaining `s=q-d` odd symbols in the final `s` positions.  Hence

\[
\boxed{
t_d^{\max}=L-s=E+d.}
\]

A contracting integerization witness exists for this Hensel depth if and only if the latest possible placement is contracting:

\[
2^{E+d}>3^d.
\]

This is equivalent to

\[
2^E>(3/2)^d,
\]

hence

\[
\boxed{
E>d\gamma.
}
\]

Because `gamma` is irrational,

\[
\boxed{
E\ge\lceil d\gamma\rceil.
}
\]

Thus the maximum Hensel denominator-clearing depth allowed by an even budget `E` is

\[
\boxed{
d_{\max}(E)=\left\lfloor\frac{E}{\gamma}\right\rfloor.}
\]

For a coefficient-surviving final prefix, `d=q` is never contracting, because that would require `2^L>3^q`, the negation of final coefficient survival.  Hence the useful Hensel levels automatically satisfy `s=q-d>=1`.

## 4. Exact three-way identification

The same irrational rotation therefore controls

\[
\boxed{
\begin{array}{c}
\text{odd-only Christoffel extra-halving rises}\\
\updownarrow\\
\text{3-adic reverse-preimage contraction budget}\\
\updownarrow\\
\text{binary alternate-prefix Hensel integerization depth}.
\end{array}
}
\]

All three use

\[
\boxed{
\gamma=\log_2(3/2)
}
\]

and differ only in whether the Beatty staircase is read forward (`floor(d gamma)`), inverted (`floor(E/gamma)`), or used as a rise indicator.

## 5. Strategic consequence

This removes one independent degree of freedom from the proof architecture.  The reverse 3-adic filter and the binary Hensel filter should not be treated as unrelated percentage losses: their admissible depths are synchronized by the same Beatty clock already present in the Christoffel reference.

The natural multiscale state is therefore indexed by one common Euclidean/Beatty phase together with the genuinely independent arithmetic channels:

\[
\boxed{
(\text{Euclidean phase},
\text{Hensel sibling maxima},
\text{ternary core address},
\text{dyadic canonical address},
\text{headroom}).
}
\]

The remaining difficulty is not to align the clocks; they are already exactly aligned.  It is to prove that a single ordinary integer cannot satisfy the synchronized cross-base address constraints at every scale.
