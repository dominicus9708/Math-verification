# MATH-109 — exact r=13 sharded AP-union closure

Date: 2026-09-13

Status: `EXACT FINITE r=13 CLOSURE / MAINLINE / FIRST CELL STILL OPEN`

## Claim scope

This result closes the audited `r=13` multi-paid negative-candidate source generated by the unchanged MATH-065 classifier/generator. It does **not** by itself close the first universal Farey cell and does **not** prove the Collatz conjecture.

## Predecessors

- MATH-107 fixed the exact `r=13` workload:
  - 1,959,535 AP cylinders;
  - 76,391,629,325 represented ordinary occurrences.
- MATH-108 established that resource sharding may repartition the exact source without changing the represented set, and repaired the bookkeeping invariant from source-record count to additive occurrence mass.
- `collatz/results/2026-09-13-math109-r13-shard-partition.tsv` independently certifies that the 16 source-record residue-class shards are disjoint and exhaustive.

## Partition certificate

The partition certificate gives

```text
TOTAL cylinders   1,959,535
TOTAL occurrences 76,391,629,325
PASS exact disjoint/exhaustive r13 16-shard source partition
```

The partition is by source-record index modulo 16, so every source record belongs to exactly one shard.

## Closure run

GitHub Actions workflow run:

```text
run id 34731274347
workflow Collatz r13 exact sharded closure gate
```

All 16 jobs `r13-shard (0)` through `r13-shard (15)` completed with conclusion `success`.
For every job, the `Run exact shard closure` step completed with conclusion `success`, after exact source export and shard selection also completed successfully.
Each job used the generalized exact AP-union engine with the additive closure invariant

```text
closed_occurrence_mass == shard_occurrence_mass
```

rather than the non-invariant number of post-split source parts.

## Conclusion

Because:

1. the 16 shards are independently certified disjoint and exhaustive over the exact MATH-107 source;
2. every shard passed the exact AP-union closure audit; and
3. closure accounting is performed with the additive represented-occurrence mass,

we may promote

```text
r = 13 : CLOSED
```

within the audited multi-paid framework.

Therefore the current multi-paid frontier becomes

```text
r >= 13 : CLOSED
2 <= r <= 12 : OPEN
```

## Claim boundary

Invalid upgrades remain prohibited:

- `r=13 CLOSED` does not imply all remaining paid-count layers are closed;
- closure of all paid-count layers would still require a separate coverage/dependency audit back to the first universal cell;
- first-cell emptiness, if eventually proved, still requires auditing the exact universal reduction/propagation to the full Collatz conjecture.

## Next step

Start with `r=12` by re-running the exact phase/address classifier and workload audit before selecting the propagation/sharding schedule. Do not assume the `r=13` workload shape persists unchanged.
