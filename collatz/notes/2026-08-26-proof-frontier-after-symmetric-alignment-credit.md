# Proof frontier after Hensel tree isometry and symmetric boundary-alignment credit

Date: 2026-08-26

Status: proof-architecture audit for the repaired Collatz line.  No Collatz proof is claimed.

## 1. Newly certified structural pieces

The current first-global-resonance branch now has four compatible exact descriptions.

### A. Anchored Christoffel compression

The mechanical gap word of length

\[
Q=72057431991
\]

is represented by an exact anchored Farey/Christoffel DAG with only

\[
\boxed{138}
\]

distinct block nodes.

### B. Hensel control as a ternary-tree automorphism

After relative discrete-log normalization, the two live Hensel control branches are exact 3-adic rooted-tree isometries.  At every ternary digit their child maps have fixed slope

\[
\boxed{-1\text{ on }t\equiv1\pmod6,
\qquad +1\text{ on }t\equiv5\pmod6.}
\]

This is a structural coordinate theorem, not an entropy reduction: each branch remains a bijection on the retained carry residues.

### C. Mechanical-alignment credit and repair bijection

A future length-`L` mechanical zero-displacement run determines one carry class modulo `3^L`:

\[
Z_L=-\sum_{i=0}^{L-1}3^i2^{e_i}\pmod{3^L}.
\]

Each zero-control step consumes exactly one matching ternary digit.  Once the alignment is exhausted, buying `L` new alignment digits by an odd repair determines exactly one class

\[
\boxed{d\pmod{2\cdot3^L}.}
\]

### D. Both exposed boundaries contain at most 44 mechanical odd ordinals of free credit

Endpoint side:

\[
\boxed{\mathfrak a_{\rm end}\le44.}
\]

Start side:

\[
\boxed{\mathfrak a_{\rm start}\le44.}
\]

Thus a nonmechanical repair/displacement is forced within 45 odd ordinals of each boundary.

## 2. Superseded sign-only target

The earlier 50-percent weighted mismatch inequality remains a valid **sufficient condition** for excluding the first resonance.  However, it is no longer the correct standalone theorem target.

For a fixed required parity, the same-parity actions

\[
d,d+2,d+4
\]

exhaust the next Hensel carry digits `0,1,2`.  Hence the control can steer future Hensel signs by paying displacement cost.

The correct target is therefore

\[
\boxed{
\text{two-boundary minimum control cost}
>4314000000,
}
\]

not an unconditional sign-discrepancy theorem.

## 3. Mechanical-scaled carry and exact circularity boundary

Let `K_m` be the terminal Hensel carry after `m` odd ordinals have been reconstructed from the endpoint backward.  Let `B_m` be the current mechanical position, with

\[
B_0=A,
\qquad
B_Q=0,
\]

and let

\[
g_m=B_m-B_{m+1}\in\{1,2\}.
\]

Define

\[
X_m:=2^{A-B_m}K_m.
\]

If the new displacement is `d_m`, then

\[
\boxed{
X_{m+1}=\frac{2^{g_m}X_m+2^{-d_m}}3.
}
\]

The exposed boundaries become ordinary:

\[
\boxed{X_0=-y,
\qquad X_Q=-N.}
\]

This is a useful two-boundary coordinate.

However, retain the previous displacement `p_m` and write

\[
X_m=-\frac{x_m}{2^{p_m}}.
\]

The ordering inequality gives

\[
d_m\ge p_m-g_m+1,
\]
so

\[
v_m:=g_m+d_m-p_m\ge1.
\]

Substitution yields

\[
X_{m+1}
=
-\frac{2^{v_m}x_m-1}{3\,2^{d_m}}.
\]

Thus, putting `p_{m+1}=d_m`,

\[
\boxed{
3x_{m+1}+1=2^{v_m}x_m.
}
\]

This is exactly the odd Collatz relation read backward.  Therefore:

\[
\boxed{
\text{tracking the full }(X,p)\text{ state step-by-step is only a coordinate rewrite of the original orbit.}
}
\]

It must not be presented as new proof content.

The genuinely new target is a theorem obtained **after aggregation/compression**: a Bellman lower bound on the 138-node block system with both boundary restrictions retained.

## 4. Why external p-adic logarithm bounds are only auxiliary here

Explicit p-adic logarithmic-form results can bound valuations such as

\[
v_p(\alpha_1^{b_1}-\alpha_2^{b_2})
\]
with logarithmic dependence on the exponent size when the algebraic bases and their heights are controlled.

A single Hensel repair congruence resembles such an approximation problem.  But the target carry in the present proof is generated by all previous controls and its arithmetic height can grow with the block.  Therefore a direct insertion of a standard p-adic logarithm theorem does not currently provide a uniform repair-length bound.

This route remains a possible local tool only after the path-dependent target has itself been compressed to bounded-height block data.

## 5. Current finite-crossing target

The exact first-resonance Bellman problem is now:

\[
\boxed{
\begin{array}{c}
\text{ordinary start interval and start credit }\le44\\
\downarrow\\
138\text{-node Christoffel block DAG}\\
\downarrow\\
\text{ternary-tree Hensel isometry}\\
+\text{ unique repair classes}\\
+\text{ one-sided displacement memory}\\
+\text{ weighted correction cost}\\
\downarrow\\
\text{ordinary endpoint interval and endpoint credit }\le44
\end{array}}
\]

and the desired inequality is

\[
\boxed{V_{\rm two-boundary}>4314000000.}
\]

Proving this closes the repaired first resonance without enumerating its `7.2e10` odd ordinals individually.

## 6. Current global target

The higher-level unifying target remains the minimal coefficient-survivor growth function

\[
\mu(K)=\min\{n:\tau_c(n)>K\}.
\]

A sufficiently strong lower growth theorem for `mu(K)` simultaneously excludes

1. an infinite coefficient-survival integer, and
2. sufficiently large paradoxical finite first crossings, when combined with the existing Archimedean first-crossing upper bound.

In canonical binary-lift language, the polynomial target `mu(K) > K^8.616` requires only that the final nonzero formation lift be forced beyond approximately

\[
8.616\log_2K
\]

rather than at positive density.  This is a useful weaker formulation of the infinite-survivor escape problem, but it is not yet proved.

## 7. DSD role

The DSD audit is doing three concrete jobs at this frontier:

- preventing a coordinate equivalence from being counted as a proof step;
- preserving the two physical/arithmetic boundaries when local Hensel blocks by themselves admit zero-cost paths;
- replacing a flat `7.2e10`-step description by a hierarchy of exact quotient states and 138 block nodes.

All proof obligations remain ordinary arithmetic statements and can be independently checked without accepting DSD as an axiom system.
