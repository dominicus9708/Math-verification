# A0 s=1 checkpoint: ordinary state is fully exposed in 73 bits

Date: 2026-08-27

Status: **SAFE exact-rational boundary theorem** inside the repaired A0 reset branch. This does not prove the Collatz conjecture.

## 1. Checkpoint

Use

\[
t_0=10J_0=104398605910,
\qquad
j_0=10R_0+1=65868186701.
\]

In the hard sector

\[
s=1,
\]

the orbit has exactly `j0` odd events by the tenth-J0 checkpoint.

Let the A0 block start at

\[
X=N+d,
\]

where the exact second-resonance branch gives

\[
N<\frac43\,2^{71}
\]

and after the certified `A0,A0,J0` reset

\[
0\le d<0.478G,
\qquad G=2^{33}.
\]

Put

\[
Z:=T^{t_0}(X).
\]

## 2. Prefix linear coefficient

The s=1 prefix coefficient is

\[
C_{\rm pre}
=\frac{3^{j_0}}{2^{t_0}}
=3\exp(-10\delta_J),
\]

where

\[
\delta_J=J_0\ln2-R_0\ln3>0.
\]

The exact logarithmic enclosure used throughout the J0/A0 audit gives

\[
C_{\rm pre}>2.99
\]

and a quantitative lower bound on the deficit

\[
3-C_{\rm pre}>0.
\]

Using `exp(x)>1+x`, the companion certificate obtains the exact rational bound

\[
3-C_{\rm pre}
>
\frac{30\delta_{J,\rm lo}}{1+10\delta_{J,\rm lo}}.
\]

When multiplied by the known lower root scale `X>2^71`, this deficit is more than enough to offset the complete prefix affine correction and the reset-strip offset.

## 3. Prefix correction bound

Let the mechanical odd-event positions be

\[
n_j-1=\left\lfloor(j-1)\log_2 3\right\rfloor.
\]

For every `j`,

\[
2^{n_j-1}\le3^{j-1}.
\]

Hence the mechanical normalized prefix correction obeys

\[
\sum_{j=1}^{j_0}\frac{2^{n_j-1}}{3^j}
\le\frac{j_0}{3}.
\]

Actual first-crossing odd positions are no later than the mechanical ones, so the actual prefix correction is no larger. After multiplying by `C_pre<3`, the contribution to `Z` is therefore strictly less than

\[
\boxed{j_0}.
\]

No statistical or Hensel assumption is used.

## 4. Exact 73-bit shell

The lower bound is immediate:

\[
Z>C_{\rm pre}X>2.99\,2^{71}>2^{72}.
\]

For the upper bound,

\[
3X<4\,2^{71}+1.434G.
\]

Subtract the certified coefficient deficit `(3-C_pre)X`, use `X>2^71`, and then add the complete prefix correction upper bound `j0`. The exact-rational certificate proves

\[
\boxed{Z<4\,2^{71}=2^{73}.}
\]

Therefore

\[
\boxed{2^{72}<Z<2^{73}.}
\]

## 5. Consequence: the internal checkpoint is an ordinary finite-address boundary

Although the checkpoint occurs after more than `10^11` accelerated time steps, its actual state is not an unbounded Hensel boundary variable.

It is one ordinary integer in the 73-bit shell

\[
\boxed{2^{72}<Z<2^{73}}.
\]

Therefore its residue modulo `2^73` is the integer itself.

This changes the state picture for the hard s=1 sector:

\[
\boxed{
X<2^{72}
\longrightarrow
Z<2^{73}
\longrightarrow
Y<2^{72},
}
\]

with all three ordinary states fully exposed by finite dyadic addresses.

## 6. Two-ended consequences for the two giant subblocks

The pre block has ordinary boundaries

\[
X<2^{72},
\qquad Z<2^{73}.
\]

Since

\[
3^{47}>2^{73},
\]

the last 47 odd ordinals of the pre block are enough, in principle, to expose its ordinary endpoint `Z` exactly by the same ternary-end argument used for the full A0 endpoint.

The tail block has

\[
Z<2^{73},
\qquad Y<2^{72}.
\]

Thus a 73-bit start address exposes `Z`, while the already-used 46-trit terminal address exposes `Y`.

So the hard A0 bridge has been converted into two enormous but **finite-two-ended** arithmetic bridges meeting at one 73-bit ordinary state.

This does not make either long bridge solved. It removes the fiction that the shared checkpoint must be represented by an unbounded 3-adic carry.

## 7. DSD audit

### SAFE

- the s=1 prefix time/odd count;
- exact coefficient `C_pre=3e^{-10 delta_J}`;
- exact-rational coefficient deficit bound;
- deterministic mechanical correction upper bound;
- `2^72<Z<2^73`;
- ordinary finite-address interpretation of the checkpoint.

### REJECTED

Do not infer from `Z<2^73` that a finite 73-bit scan of all possible `Z` proves the long pre/tail bridges. The bridge languages remain structured long-word problems.

Do not replace the physical checkpoint state by an arbitrary 73-bit integer. It must be generated by the pre bridge and consumed by the tail bridge for the same `Z`.

## 8. Next gate

Define exact boundary maps

\[
\mathcal Z_{\rm pre}(X)
\]

and

\[
\mathcal Z_{\rm tail}(Y)
\]

for the two subblocks, then test whether their reachable ordinary checkpoint sets intersect inside

\[
(2^{72},2^{73}).
\]

The Christoffel/Farey DAG and reachable-Xi composition can now be used with an ordinary 73-bit checkpoint anchor rather than an arbitrary carry state.

Companion exact certificate:

`collatz/src/A0_s1_checkpoint_state_73bit_exposure_certificate.py`.
