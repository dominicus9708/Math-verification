# Terminal three-defect transfer progress in the `1000` branch

Date: 2026-08-10

Status: **FINITE EXACT REDUCTIONS + PARTIAL EXACT EXCLUSION FOR THREE TERMINAL DEFECTS**

This note continues the isolated first-crossing analysis at

\[
(q,\sigma)=
(137,528,045,312,\;217,976,794,617)
\]

inside the `m=46`, high-four-trit `1000` recursive-sufficiency branch.

The previous result proved that the last 20 odd-position coordinates must contain at least three positive mechanical-cap defects.  The present note begins the `exactly three` transfer analysis.  It does **not** yet eliminate every three-defect configuration and does not prove Collatz globally.

## 1. Terminal defect process

Let

\[
z_i=\kappa_i-d_i\ge0,
\qquad
\kappa_i=\lfloor i\log_2 3\rfloor.
\]

The exact last-20 mechanical gap pattern is

\[
\boxed{
1,2,2,1,2,1,2,1,2,2,1,2,1,2,2,1,2,1,2.
}
\]

The transition constraint is

\[
z_{i+1}\le z_i+(\kappa_{i+1}-\kappa_i)-1.
\]

Therefore a new positive-defect run can start from zero only across a gap-two transition, and it starts with amplitude `1`.

For exactly three positive terminal coordinates, the positive set is a union of runs whose lengths form one of

\[
3,\qquad2+1,\qquad1+2,\qquad1+1+1.
\]

Only a run beginning at the first terminal coordinate can carry a large amplitude inherited from earlier odd positions.

## 2. No inherited amplitude: complete finite exclusion

Set the first terminal amplitude to

\[
z_0=0.
\]

The finite transfer generated by the exact gap pattern has exactly

\[
\boxed{275}
\]

amplitude sequences with exactly three positive coordinates.

Their largest local amplitude is only

\[
\boxed{2}.
\]

For each sequence the last-48 correction was evaluated exactly modulo `3^48`, giving the ordinary endpoint because the existing endpoint ceiling is below `3^48`.

The endpoint was compared against the exact `1000` start family

\[
x=4\left(3^{46}+3^{43}+\sum_{i=0}^{39}a_i3^i\right)+3,
\qquad a_i\in\{0,1\},
\]

with the certified near-return window

\[
0\le y-x\le29,785,654.
\]

A ternary Cantor predecessor test finds

\[
\boxed{0/275}
\]

compatible starts.

Hence an exactly-three candidate must have

\[
\boxed{z_0>0}.
\]

The exact verifier is

`collatz/src/terminal_three_defect_transfer.py`.

It certifies the last-48 mechanical floors using rational intervals for `ln 2` and `ln 3`; no floating logarithm is used in the proof check.

## 3. Inherited run of length one

Now assume

\[
z_0>0,\qquad z_1=0.
\]

The remaining two positive coordinates are local.  The finite tail transfer gives exactly

\[
\boxed{65}
\]

local amplitude templates.

Because the first terminal contribution carries a factor `3^19`, the inherited amplitude does not change `y mod 3^19`.

The lower-19 near-return/Cantor congruence alone eliminates

\[
\boxed{16/65}
\]

templates for every inherited amplitude.

The remaining `49` templates split into `58` carry records.  Each record reduces to one bounded discrete-log congruence

\[
\boxed{
B2^{-z}\equiv4S_{21}+K\pmod{3^{29}},
}
\]

where `S_21` is a 21-trit `{0,1}` Cantor coordinate.

Using the certified global inherited-amplitude ceiling, a fast exact base-two logarithm modulo `3^20` followed by an exact lift/check modulo `3^29` gives

- `2,980,125` low-modulus amplitude candidates;
- exactly `317` full `3^29` hits;
- `48` local templates with at least one full hit;
- `5,547,192` ordinary starts after restoring all lower-19 choices, counted with terminal-pattern multiplicity.

Every one of these starts was then iterated with the accelerated Collatz map using an exact rationally certified coefficient-barrier table

\[
a_k=\lceil k\log_3 2\rceil,
\qquad k\le1000.
\]

The result is

\[
\boxed{
\max\tau_c=294
}
\]

at

\[
\boxed{
x=36,788,872,125,537,377,030,143.}
\]

No start survives coefficient contraction through depth `1000`, and no 128-bit overflow occurs.

Therefore exactly three terminal defects cannot have an inherited first run of length one.

Hence every exactly-three candidate must satisfy

\[
\boxed{z_0>0,\quad z_1>0.}
\]

## 4. Remaining run geometries

After Sections 2--3, only two structural families remain:

### A. Inherited run of length two plus one local defect

\[
(z_0,z_1)>0,
\]

followed by one local isolated defect.  The local defect can occur at exactly the terminal positions

\[
\boxed{3,5,7,9,10,12,14,15,17,19.}
\]

The first two amplitudes form the same two-variable adjacent-run discrete-log structure already used in the exactly-two-defect analysis; the local defect changes only the fixed terminal residue/translation constants.

### B. Inherited run of length three

The first three terminal coordinates are all positive and form one inherited run.  This remains a genuinely three-amplitude terminal problem before factoring out a common power of two.

## 5. Family A: local defect at terminal position 15

For the geometry with the third defect at terminal position `15`, the lower-18 congruence forces

\[
\boxed{d\ge29,575,242.}
\]

Only `1,024` lower-18 Cantor choices survive.

An exact `3^30` bounded discrete-log scan of the inherited adjacent pair gives

- `11,038` amplitude/high-state hits;
- `11,015` distinct high ternary start states.

Restoring the `1,024` lower choices yields exactly

\[
\boxed{11,279,360}
\]

ordinary starts in the safe superset.

Every start was checked with the exact coefficient-barrier table.  The maximum is

\[
\boxed{
\tau_c=343
}
\]

at

\[
\boxed{
x=36,772,210,855,782,522,928,767.}
\]

Thus this geometry is completely excluded.

## 6. Family A: local defect at terminal position 17

For terminal position `17`, the lower-18 congruence gives

\[
\boxed{d\ge27,024,948}
\]

and exactly `6,144` lower-18 Cantor choices.

The corrected Denjoy--Koksma/Ostrowski amplitude cost and safe rational crossing bounds give the inherited first-amplitude ceiling

\[
\boxed{z_0\le6,478,921.}
\]

The bounded discrete-log transfer gives

- `320,343` raw amplitude/high-state hits before the start-dependent budget filter;
- `272,359` safe hits after the filter;
- `252,997` distinct safe high ternary states.

The high states occupy all 16 possible upper-four `{0,1}` ternary patterns, so no additional upper-four prefix is empty.

Restoring all lower choices gives

\[
oxed{1,554,413,568}
\]

ordinary starts.

They were split into the 16 upper-four ternary branches and checked exactly.  The branch maxima are:

| upper four trits | starts | max `tau_c` |
|---|---:|---:|
| `0000` | 103,968,768 | 430 |
| `0001` | 65,796,096 | 406 |
| `0010` | 14,395,392 | 332 |
| `0011` | 2,457,600 | 314 |
| `0100` | 115,740,672 | 395 |
| `0101` | 112,257,024 | 430 |
| `0110` | 114,216,960 | **484** |
| `0111` | 113,836,032 | 417 |
| `1000` | 114,843,648 | 376 |
| `1001` | 112,078,848 | 403 |
| `1010` | 113,178,624 | 376 |
| `1011` | 115,636,224 | 436 |
| `1100` | 113,405,952 | 390 |
| `1101` | 113,952,768 | 384 |
| `1110` | 114,554,880 | 392 |
| `1111` | 114,094,080 | 367 |

The global maximum is therefore

\[
\boxed{
\max\tau_c=484.
}
\]

No start survives to depth `1000` and no trajectory overflows the exact 128-bit scan range.

The `j=15` maximizing start and all 16 `j=17` branch-maximizing starts were independently rechecked in Wolfram using direct arbitrary-precision integer comparisons `3^q < 2^k`.  The returned stopping times were

\[
\boxed{
343,
314,332,406,430,395,430,484,417,
376,403,376,436,390,384,392,367
}
\]

in the recorded order, exactly matching the C++ scans.

Hence the `j=17` geometry is also completely excluded.

## 7. Current exactly-three frontier

Exactly-three terminal defects are now restricted to:

1. an inherited first run of length two plus one local defect at one of
   \[
   \boxed{3,5,7,9,10,12,14,19};
   \]
2. one inherited first run of length three.

The local positions `15` and `17` have been removed exactly.

The next efficient move is to reuse the same two-variable adjacent-run transfer for the remaining eight local positions, ordered by their branch-dependent near-return/correction budget, before tackling the length-three inherited run.

## 8. Scope

The present results are finite, branch-specific exclusions inside the isolated first-crossing resonance.  They establish neither the full elimination of the `1000` branch nor the Collatz conjecture.

Their methodological significance is that the terminal transfer has changed the three-defect problem from a combinatorial choice of terminal coordinates into a small set of inherited-run families with one, two, or three amplitude variables, and two of the ten two-variable geometries have already been closed by exact ordinary-integer verification.
